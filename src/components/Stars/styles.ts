import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  canvas: {
    width: 257,
    height: 249,
    position: 'absolute',
    zIndex: 1
  },
  container: {
    justifyContent: 'flex-end',
    alignItems: 'center'
  }
})